Se encuentra problermas cpn la pregunta 0298: 

El archivo question-0002.txt solicita impleentar una función `segmentar_clientes_kmeans(datos, num_clusters)` que debe devolver 
`(labels, centroids)` utilizando K-Means; Sin embargo, el archivo `question-0002-usecase-generator.py` presenta inconsistencias con el enunciado y con el formato esperado por el validador.

El generador:

- no construye explícitamente pares
  (input_data, output_data),
- no utiliza sklearn.cluster.KMeans,
- no calcula centroides,
- y genera etiquetas aleatorias independientes de los datos.

A demás, retorna `(info, resultado)` donde resultado es un DataFrame con una columna "Cluster", en lugar de retornar la salida esperada de la función `(labels, centroids)`.

El notebook validador clasifica este caso como "usecase generator error". Personalmente creo y confio en que la solución implementada sigue fielmente las especificaciones del enunciado original.
